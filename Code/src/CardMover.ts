import { Card } from "./Card/Card"
import { Stack } from "./Stack"

export class CardMover {

    public moveCard(s1:Stack, s2:Stack, c:Card) {
        // ..
        // Revoir la logique sur le déplacement des cartes
    }
}