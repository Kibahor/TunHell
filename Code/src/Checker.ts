import { Card } from "./Card"
import { Stack } from "./Stack"
import { Player } from './Player/Player'

export class Checker {

    public checkCardToStack(p:Player, s:Stack, c:Card) : Boolean {
        // Va y avoir du boulot ici, c'est la partie la plus chiante !!
        return
    }
}