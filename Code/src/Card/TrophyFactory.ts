import { CardFactory } from "./CardFactory";


export class TrophyFactory extends CardFactory {
    
    public CreateCard(receipe:JSON, nb:number ): Array<Card> {

    }

    private verify_receipe() : boolean{

    }
}